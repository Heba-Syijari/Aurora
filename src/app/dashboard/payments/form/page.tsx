// sections
import { PaymentFormView } from 'src/sections/payments/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Payment Form',
};

export default function Page() {
  return <PaymentFormView />;
}
