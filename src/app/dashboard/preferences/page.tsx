import { UserPreferencesView } from 'src/sections/preferences/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Preferences',
};

export default function Page() {
  return <UserPreferencesView />;
}
